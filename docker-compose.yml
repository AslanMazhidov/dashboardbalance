services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - DATABASE_URL=file:/app/data/balance.db
      - AUTH_SECRET=${AUTH_SECRET}
    volumes:
      - db-data:/app/data
    restart: unless-stopped

volumes:
  db-data:
